# Karim Python Reference

Source: local remote-tracking ref `origin/karim` at commit `727254d7266791987de17b2fa92d84a09f602c07`.

This branch is no longer listed by the live remote heads, but remains available in
this local clone. It is preserved here because it contains the earlier standalone
Python CLI implementation that informed later TypeScript behavior.

Included:
- `generate_site.py` - standalone intake, prompt, mockup, planning, image, HTML repair,
  build, and zip flow.
- `hyperui_reference.txt` - optional design reference text loaded by the CLI.

Excluded:
- `Testing websites/` generated HTML, image assets, zip files, and `.DS_Store`.
  These are output artifacts, not source. Representative fixtures can be selected
  later if tests need them.

